# Vercel Env Values

Copy `.env.example` to `production.env`, then manually fill in your real values.

```text
vercel-env/
  .env.example
  production.env
```

`production.env` is ignored by Git, so it is safe to use locally as a private checklist.

Do not commit real secrets to GitHub.

